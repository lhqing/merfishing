"""
Using the bigfish package to process smFISH image, call spots.

TODO add call spot function, the function take a numpy array as input and
return spot dataframe as the default vizgen analysis did, saving the result to hdf store for later assign to cell.
"""
